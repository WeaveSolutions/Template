// Export the MobileAuth class which now extends Auth0MobileAuth
export { MobileAuth } from './auth-mobile';

// Also export Auth0MobileAuth directly if needed
export { Auth0MobileAuth } from './auth0-mobile-auth';
